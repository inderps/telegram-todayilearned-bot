import getTILPost from './../src/getTILPost';
import { describe, beforeEach, it } from 'mocha';

describe('getTILPost', () => {
  beforeEach(() => {
  });

  it('should return the whole element as patch if node type is different', () => {
    console.log(getTILPost);
  });
});
